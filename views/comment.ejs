<!DOCTYPE html>
<header>
<!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">

<nav class="navbar navbar-expand-sm bg-light">

    <div class="container-fluid">
      <!-- Links -->
      <a class="navbar-brand" href="/log">SportyNews</a>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/comment">Comment</a>
        </li>
        
        
      </ul>
    </div>
  
  </nav>

</header>
<body>
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
        <div class="container bg-light text-dark">
            <form action="" method="POST">
                
                <div class="form-group">
                    <label for="comment">Komentar:</label>
                    <textarea class="form-control" name="comment" id="comment" rows="3"></textarea>
                </div>
                
                
                
                <button type="submit" class="btn btn-secondary">Comment</button>
            </form>
        </div>
    </div>

</body>
<script>
    /*function handleSubmit(event){
        event.preventDefault();
        const data = new FormData(event.target);
        const comment = data.get('comment');
        var indComment = document.getElementById('numOfComments')+1;
        var userNick = document.getElementById('username');
        var userMail = document.getElementById('currentUser');
        var obj = {
            id:indComment,
            author:userNick,
            email:userMail,
            comment:comment
        }

        const fs = require("fs");
        let commentssJson = fs.readFileSync("../komentari.json","utf-8");
        let commentss = JSON.parse(commentssJson);
        commentss.push(obj);
        commentssJson = JSON.stringify(commentss);
        fs.writeFileSync("../komentari.json",commentssJson,"utf-8");

    }

    const form = document.querySelector('form');
    form.addEventListener('submit', handleSubmit);

    const formEl = document.querySelector('.form');
    formEl.addEventListener('submit', event =>{
        event.preventDefault();
        const formData = new FormData(formEl);
        const data = Object.fromEntries(formData);
        

    });*/
</script>